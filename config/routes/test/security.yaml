Security-login:
  path: /security/login
  controller: App\View\Controller\DevelopmentLoginController::loginAction

# This doesn't fixes anything... because of reasons
Security-logout:
  path: /security/logout
  controller: Symfony\Bundle\FrameworkBundle\Controller\RedirectController
  defaults:
    route: Administration-landing
    permanent: true